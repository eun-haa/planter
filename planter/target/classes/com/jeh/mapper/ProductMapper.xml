<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeh.mapper.ProductMapper">
	<!-- 1-1.상품 등록 -->
	<insert id="productReg">
		INSERT INTO product(pname, pprice, pstock, pmemo, cid, pregdate)
		VALUES(#{pname}, #{pprice}, #{pstock}, #{pmemo}, #{cid}, NOW())
	</insert>
	<!-- 1-2.파일 첨부 -->
	<insert id="insertSelectKey">
		<selectKey keyProperty="pno" order="BEFORE" resultType="int">
			<!-- 제일 큰 pno 값에 +1을 해주어 새로운 new max pno -->
			SELECT MAX(pno)+1 pno FROM product
			
		</selectKey>
		INSERT INTO product(pno, pname, pprice, pstock, pmemo, cid, pregdate)
		VALUES(#{pno}, #{pname}, #{pprice}, #{pstock}, #{pmemo}, #{cid}, NOW())
	</insert>
	
	<!-- 2.상품 목록 -->
	<select id="shopList" resultType="com.jeh.domain.ProductDTO">
		SELECT * FROM product
	</select>

	<!-- 3.상품 상세 -->
	<select id="shopDetail" resultType="com.jeh.domain.ProductDTO">
		SELECT * FROM product
		WHERE pno=#{pno}
	</select>
	
	<!-- 4.상품 수정 -->
	<update id="postModify">
		UPDATE product
    	SET cid=#{cid}, pname=#{pname}, pprice=#{pprice},
    		pdiscount=#{pdiscount}, pstock=#{pstock}, pmemo=#{pmemo},
    		pupdate=NOW()
    	WHERE pno=#{pno}
	</update>
	
	<!-- 5.상품 삭제 -->
    <delete id="delete">
    	DELETE FROM product
    	WHERE pno=#{pno}
    </delete>
</mapper>